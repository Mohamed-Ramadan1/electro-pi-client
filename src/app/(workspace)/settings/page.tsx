import { Settings } from "lucide-react";
import { PlaceholderPage } from "@/components/workspace/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      icon={Settings}
      title="Settings"
      description="Configure your workspace preferences, notifications, and account settings. Coming soon."
    />
  );
}
